export enum GateStatus {
    not_started = "not_started",
    in_progress = "in_progress",
    completed = "completed"
}

export enum GatePublishedStatus {
    not_published = "not_published",
    paused = "paused",
    published = "published"
}