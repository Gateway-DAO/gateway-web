import * as Styled from './style'
const SubmitButton = (props)=>{
    return(
    <Styled.Wrapper>
        <Styled.SubmitButton>
            SUBMIT
        </Styled.SubmitButton>
    </Styled.Wrapper>
);
};

export default SubmitButton ;